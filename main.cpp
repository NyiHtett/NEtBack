#include<iostream>
using namespace std;

void scramble(string str)
{
    
}

void swap(string s)
{
   string newString;
   int i = 0;
   while(i< s.length)
   {
      if(true)
      newString += s[i];
   }
}

int main()
{
    string res = "";
    cout << "assign a line: ";
    getline(cin, res);
    scramble(res);
    swap(res);
    return 0;
}

